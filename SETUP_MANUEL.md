# DWELL — Setup

Tout a ete configure automatiquement :
- [x] Clone du repo `https://github.com/Ttui81/DWELL.git`
- [x] Extensions VS Code installees (Live Server, Prettier, HTML CSS Support, Auto Rename Tag, Path Intellisense, GitLens)
- [x] Git configure (nom : Romeo Maigraij Botta, email : romeomaigray@gmail.com)
- [x] Commande `code` ajoutee au PATH (relance ton terminal pour que ca prenne effet)

---

## A faire manuellement

### 1. Obtenir l'acces au repo GitHub (si pas encore fait)

- Envoie ton **nom d'utilisateur GitHub** a Matteo
- Attends son invitation par email
- **Accepte l'invitation** depuis l'email GitHub

> Sans cette etape, tu ne pourras pas `git push`.

---

## Workflow quotidien (rappel)

```bash
# Avant de commencer
git pull origin main

# Apres tes modifications
git add .
git commit -m "Description de ce que tu as change"
git push origin main
```

Les fichiers principaux sont dans `01_WEBSITE/`.
